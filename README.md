# Wilson-Cowan-Phase
Applying phase reduction to the Wilson-Cowan model and comparing result to the full one. The main code can be found in `WilsonCowanRing`, where one can also find a more detailed `README`. All the MATLAB code is in `MATLAB`.
